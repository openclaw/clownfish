---
repo: "openclaw/openclaw"
cluster_id: "pr-inventory-mixed-20260615T164007-063"
mode: "plan"
run_id: "27561808927"
run_url: "https://github.com/openclaw/clownfish/actions/runs/27561808927"
head_sha: "45b1d8be7148b107b47414b510f5077d67a9d23e"
workflow_conclusion: "failure"
result_status: "blocked"
published_at: "2026-06-15T16:54:15.699Z"
canonical: null
canonical_issue: null
canonical_pr: null
actions_total: 40
fix_executed: 0
fix_failed: 0
fix_blocked: 0
apply_executed: 0
apply_blocked: 0
apply_skipped: 0
needs_human_count: 1
---

# pr-inventory-mixed-20260615T164007-063

Repo: openclaw/openclaw

Run: [https://github.com/openclaw/clownfish/actions/runs/27561808927](https://github.com/openclaw/clownfish/actions/runs/27561808927)

Workflow conclusion: failure

Worker result: blocked

Canonical: unknown

## Summary

Plan blocked: the cluster preflight artifact could not hydrate live GitHub state for any listed PR because GitHub returned installation API rate-limit HTTP 403 errors. No canonical exists for this inventory shard, and no mutating close/comment/label action is safe without live kind/state/updated_at.

## Impact

| Metric | Count |
| --- | ---: |
| Worker actions | 40 |
| Fix executed | 0 |
| Fix failed | 0 |
| Fix blocked | 0 |
| Applied executions | 0 |
| Apply blocked | 0 |
| Apply skipped | 0 |
| Needs human | 1 |

## Fix Execution Actions

| Action | Status | Target | Branch | Reason |
| --- | --- | --- | --- | --- |
| _None_ |  |  |  |  |

## Apply Actions

| Target | Action | Status | Classification | Reason |
| --- | --- | --- | --- | --- |
| _None_ |  |  |  |  |

## Worker Action Matrix

| Target | Action | Status | Classification | Reason |
| --- | --- | --- | --- | --- |
| #87081 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87102 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87111 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87247 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87255 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #93210 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87273 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #84896 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87300 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87304 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87349 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87373 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87393 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87434 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #93179 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87478 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87481 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87521 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87526 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87527 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87528 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87529 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87530 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87545 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87552 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87560 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87568 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87572 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87592 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87601 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87617 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #91988 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87759 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87784 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87793 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87831 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87888 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87912 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87937 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |
| #87955 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify or plan a mutating action. |

## Needs Human

- Re-run cluster preflight after GitHub installation API rate limit clears; all 40 listed refs have kind=unknown, state=unavailable, and updated_at=null in the provided artifact, so no per-PR classification or close/comment/label plan is auditable.
