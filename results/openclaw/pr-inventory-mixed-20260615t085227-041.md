---
repo: "openclaw/openclaw"
cluster_id: "pr-inventory-mixed-20260615T085227-041"
mode: "plan"
run_id: "178151585729"
run_url: null
head_sha: null
workflow_conclusion: null
result_status: "needs_human"
published_at: "2026-06-15T09:36:30.195Z"
canonical: null
canonical_issue: null
canonical_pr: null
actions_total: 25
fix_executed: 0
fix_failed: 0
fix_blocked: 0
apply_executed: 0
apply_blocked: 0
apply_skipped: 0
needs_human_count: 2
---

# pr-inventory-mixed-20260615T085227-041

Repo: openclaw/openclaw

Run: unknown

Workflow conclusion: unknown

Worker result: needs_human

Canonical: unknown

## Summary

All 25 PR inventory refs are blocked from safe classification because the cluster preflight artifact has unavailable live state for every item after GitHub API rate limiting. No close, label, comment, merge, or fix action is planned.

## Impact

| Metric | Count |
| --- | ---: |
| Worker actions | 25 |
| Fix executed | 0 |
| Fix failed | 0 |
| Fix blocked | 0 |
| Applied executions | 0 |
| Apply blocked | 0 |
| Apply skipped | 0 |
| Needs human | 2 |

## Fix Execution Actions

| Action | Status | Target | Branch | Reason |
| --- | --- | --- | --- | --- |
| _None_ |  |  |  |  |

## Apply Actions

| Target | Action | Status | Classification | Reason |
| --- | --- | --- | --- | --- |
| _None_ |  |  |  |  |

## Worker Action Matrix

| Target | Action | Status | Classification | Reason |
| --- | --- | --- | --- | --- |
| #88789 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #82950 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #92000 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #91245 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #91376 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #90989 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #92006 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #90918 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #91240 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #72025 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #85104 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #91427 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #79702 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #92012 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #92014 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #90731 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #92002 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #59801 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #59859 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #59382 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #59920 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #59517 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #59771 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #92165 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |
| #91208 | needs_human | blocked | needs_human | Live GitHub state is unavailable, so the worker cannot safely classify this PR independently or plan a mutation. |

## Needs Human

- Re-run hydration for all 25 refs after GitHub API rate limit clears; this artifact has no live target kind, state, updated_at, comments, review comments, files, or checks for any candidate.
- No closure, label, comment, merge, or fix action is safe from this run because every candidate's live state is unavailable.
