interface:
  display_name: "ProjectClownfish Cluster Workflow"
  short_description: "Run and scale guarded cluster cleanup"
  default_prompt: "Use $projectclownfish-cluster-worker to review runs, tune guardrails, and dispatch safe canary cluster jobs."

policy:
  allow_implicit_invocation: true
